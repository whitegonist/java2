# 202530124 이채원



## 0318 강의

### 절차 지향 언어

프로그램을 **절차, 순서에 따라 실행**
**데이터**와 **함수**를 분리
코드 **유연성 부족**, **재사용 어려움**
**전역 변수를 많이 사용하기** 때문에 코드의 **가독성과 유지보수가 어려움**

C, Pascal, Fortran 등이 있음

*** 


### 객체 지향 언어 

**현실의 객체를 모델링**하여 프로그램을 작성함
**데이터**와 **함수**를 분리
객체는 **데이터와 데이터를 처리하는 메소드를** 모두 포함
상속, 캡슐화, 다형성 등의 개념을 활용하여 **유연하고 재사용 가능한** 코드를 작성 가능

Java, C++, Python

***


### 함수 지향 언어

함수형 언어는 **함수를 일급 객체로 취급**하고, **상태 변경을 피하고 불변성을 지향하는 프로그래밍 패러다임**입니다.

함수형 언어에서는 **함수의 조합으로 복잡한 작업을 수행**하며 **상태 변경 대신에 데이터를 변환하는 방식**으로 프로그램을 작성합니다.

**재귀, 고차 함수, 순수 함수 등의 개념을 중요시**하며, **병렬 처리와 높은 수준의 추상화를 지원**합니다.

함수 지향 언어는 **객체 지향 언어의 특성을 그대로** 가져가되, **함수 지향 적인 기능을 추가**한 것입니다.

Python, Kotlin, Haskell, Lisp, Scala

***

### 자바의 플랫폼 독립성, WORA

#### WORA(Write Once Run Anywhere)
* **한 번 작성된 코드**는 OS, H/W에 상관없이 **모든 플랫폼에서 바로 실행**되는 자바의 특징

* C/C++ 등 기존 언어가 가진 플랫폼 종속성 극복

* **네트워크에 연결된 어느 클라이언트 에서나 실행** 웹 브라우저, 분산 환경 지원

#### WORA를 가능하게 하는 자바의 특징

##### 바이트 코드(byte code)
 - 자바 소스를 **컴파일한 목적 코드**
 - CPU에 종속적이지 않은 **중립적인 코드**
 - **JVM에 의해 해석되고 실행**됨

##### JVM(Java Virtual Machine)
 - 자바 바이트 코드를 실행하는 자바 JVM(소프트웨어)


 ## 0325 강의

### 자바의 특성

#### 가비지 컬렉션

- 자바 언어는 **메모리 할당 기능은 있어도 메모리 반환 기능 없음**
- 사용하지 않는 메모리는 JVM에 의해 자동 반환

***

클래스는 전부 대문자로 시작

자바 주석 다는법
``` java 
// 주석

/* 주석
입니다
*/

컨트롤 + 슬래시
// 주
// 석
```


### 상수 선언
final 키워드 사용
선언 할 때 초기값 지정
실행 중 값의 변경 불가능

``` java 
final double PI = 3.141592;
```


## 0401 강의

### 1. 배열 기본
- 같은 타입 데이터를 연속 저장하는 자료구조
- 인덱스는 **0부터 시작**
- 선언: `int[] arr;`
- 생성: `arr = new int[5];`
- 초기화: `int[] arr = {1,2,3};`


### 3. 배열 사용 시 주의
- 인덱스 범위: `0 ~ 길이-1`
- 음수/초과 인덱스 → 오류
- 생성 전에 접근 → 오류

---

### 4. 반복문
- `for` : 반복 횟수 명확할 때
- `while` : 조건 중심 반복
- `do-while` : 최소 1번 실행

#### 제어문
- `break` : 반복문 즉시 종료
- `continue` : 다음 반복으로 건너뜀

---

### 5. 조건문
- `if / else if / else`
- `switch` : 값 비교 (정수, 문자, 문자열)
  - `break` 없으면 계속 실행됨

---

### 6. 비트 연산
- AND(`&`), OR(`|`), XOR(`^`), NOT(`~`)
- 시프트: `<<`, `>>`
- 빠른 연산, 플래그 처리, 최적화에 사용

---
